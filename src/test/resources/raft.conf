server {
  election.timeout.min.ms = 1000
  election.timeout.max.ms = 1500
  heartbeat.interval.ms = 300
  log.dir = /tmp/raft-logs
}

cluster {
  quorum = 2
  precursor = "localhost:5000"
  address.resolution.timeout.ms = 5000
}
