akka {
  log-dead-letters = 0
  # log-dead-letters-during-shutdown = off # not working
  actor {
    provider = local
  }
  remote {
    enabled-transports = ["akka.remote.netty.tcp"]
    netty.tcp {
      hostname = "127.0.0.1"
      port = 2552
    }
  }
}